import { MaterialCommunityIcons } from "@expo/vector-icons";

const BarcodeScannerIcon = ({ color = "white" }) => {
  return <MaterialCommunityIcons name="barcode-scan" color={color} />;
};

export default BarcodeScannerIcon;
